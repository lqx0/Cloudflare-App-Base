# Cloudflare-App-Base 产品规格

## 1. 目的

本文档定义可复用 `main` 基线，而不是某个具体客户产品。具体业务必须在 `project/<name>` 分支建立独立规格。

## 2. 必须保留的能力

1. React/Vite 单页应用与 Hono Worker 同源运行。
2. Better Auth 邮箱密码注册、登录、Session 和受保护路由。
3. Google OAuth 集成能力；只有项目提供有效凭据时启用。
4. D1、Kysely、迁移和本地数据工具。
5. 条件式邮件 Provider、验证链接和密码重置能力。
6. Local、Preview、Production 配置及部署准备。
7. 数据库备份、恢复、Seed、Time Travel 和认证 CLI。
8. 响应式共享布局、主题、基础错误页面和无障碍基础。
9. 聚焦测试、Lint、Build、类型检查和安全响应头。
10. 中英文配套文档。

## 3. 基础数据

主分支仅维护 Starter 账户模型：用户、账户、Session 和验证数据。邮箱是唯一账户身份。不得在主分支加入产品、订单、支付、地址、招聘、测验或其他业务模型。

## 4. 项目分支责任

每个 `project/<name>` 必须自行定义：品牌和域名、用户角色、业务页面、业务数据、隐私和保留政策、邮件用途、远程资源名、验收标准及未来功能提示。

## 5. 安全与授权

基础仓库不得提交 Secret。远程 Worker/D1、迁移、Secret、OAuth、DNS、邮件、付费资源和删除操作需要针对目标环境的明确授权。保留工具不代表允许执行。

## 6. 非目标

主分支不提供客户内容、最终法律文本、通用后台生成器、多租户平台、电子商务或所有未来项目的抽象业务框架。

## 7. 验收

主分支变更必须保持中英文文档一致，通过聚焦测试、完整测试、Lint 和 Build，并报告 Git 分支、提交、推送和任何未执行的远程操作。

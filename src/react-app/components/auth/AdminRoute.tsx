import type { ReactNode } from "react"; import { useEffect,useState } from "react"; export function AdminRoute({children}:{children:ReactNode}){const [allowed,setAllowed]=useState<boolean|null>(null);useEffect(()=>{void fetch("/api/admin/questions").then(r=>setAllowed(r.ok)).catch(()=>setAllowed(false));},[]);if(allowed===null)return null;if(!allowed)return <section className="mx-auto max-w-xl px-6 py-16"><h1 className="text-3xl font-bold">Administrator access required</h1><p>You do not have permission to view this page.</p></section>;return <>{children}</>;}

import type { AdminQuestion } from "../admin-questions-api"; export function QuestionList({ questions }: { questions: AdminQuestion[] }) { return <section><h2 className="text-2xl font-semibold">Question bank</h2><ul className="mt-4 space-y-3">{questions.map(q=><li key={q.id} className="rounded-lg border p-4"><p className="font-medium">{q.prompt}</p><p className="text-sm text-muted-foreground">{q.type.replace(/_/g, " ")}</p></li>)}</ul><p className="mt-4">Question editing, deletion, and availability controls will be implemented in the production version.</p></section>; }

import { useEffect,useState } from "react"; import { QuestionForm } from "@/features/admin/components/QuestionForm"; import { QuestionList } from "@/features/admin/components/QuestionList"; import { listQuestions,type AdminQuestion } from "@/features/admin/admin-questions-api"; export function QuestionBankPage(){const [questions,setQuestions]=useState<AdminQuestion[]>([]);const [error,setError]=useState("");useEffect(()=>{void listQuestions().then(setQuestions).catch(e=>setError(e instanceof Error?e.message:"Unable to load questions"));},[]);return <main className="mx-auto max-w-4xl space-y-8 px-6 py-16"><h1 className="text-4xl font-bold">Question management</h1>{error&&<p role="alert">{error}</p>}<QuestionForm onCreated={q=>setQuestions(current=>[q,...current])}/><QuestionList questions={questions}/></main>;}

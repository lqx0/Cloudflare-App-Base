# Cloudflare-App-Base Agent 指令

修改仓库前阅读本文件。

同时阅读全部中英文项目文档，以及 Starter 原始安装和 Agent 文档。

## 首要规则

只要没有重大冲突、安全或隐私问题、交付阻碍或用户明确要求，就保留 `lqx0/cloudflare-fullstack-starter` 原始行为。

不要为了个人偏好替换正常工作的基础设施。

## 文档语言流程

默认 Markdown 文件名为英文，中文配套文件使用 `.zh-CN.md`。

内部流程：

1. 先确认中文语义。
2. 更新中文文档。
3. 同步英文文档。
4. 检查语义一致。
5. 不在公开文档正文中加入语言优先级声明。

同一变更中更新两种语言。

## 第一次执行边界

Codex 第一次执行只限本地。

允许：

- 检查仓库和 Git
- 阅读文件
- 安装依赖
- 审查并运行本地初始化
- 创建本地环境文件和 Secret
- 应用本地迁移
- 运行本地应用
- 运行 lint、build、check 和本地测试
- 创建或合并文档
- 在里程碑 0 和 1 内进行非破坏性本地修改

未经明确批准禁止：

- Preview 或 Production 部署
- 远程 Worker 或 D1 修改
- Preview 或 Production 迁移
- 远程 Secret 同步
- 创建 Google OAuth 凭据
- 修改 GoDaddy 或 Cloudflare DNS
- 配置域名邮箱
- 创建付费资源
- 远程删除或破坏性操作

不要删除提供这些能力的脚本。限制的是执行。

## 范围

第一阶段包括公开企业页面、邮箱密码账户、Google 登录能力、受保护 Account/Profile、Privacy、Terms、SEO 和响应式设计。

第一阶段不包括网店、支付、订单、库存、商家后台、用户导出、Astro 和多租户。

不得创建推测性的第二阶段模块。

## 身份验证

使用 Better Auth 和 Starter 实现。

保留模型、迁移、邮箱密码、Session、Google Provider、验证链接 Hook、密码重置 Hook 和 CLI 管理。

不得重写密码哈希、构建数字 OTP、按不可信邮箱自动合并账户、暴露 Secret，或让 Google 凭据成为邮箱密码登录必需条件。

第一阶段隐藏或禁用普通用户删除账户入口，但保留底层和 CLI 能力。

## 数据

不要添加 `registrations` 表。已批准模型是用户账户系统。

只收集最少资料。

不要添加产品、订单、库存、地址、支付或敏感身份字段。

不要添加网页用户列表或导出。

## 已有远程功能

保留部署、Secret 同步、远程迁移、备份、恢复、Time Travel、邮件 Provider、用户管理和远程删除，除非确认存在安全问题。

功能存在不代表允许执行。

## 修改流程

编辑前：

1. 检查当前实现。
2. 阅读附近测试和惯例。
3. 找出最小正确改动。
4. 尽量保留上游行为。
5. 对修改行为添加或更新聚焦测试。
6. 运行相关现有验证脚本。
7. 审查 `git diff`。

避免无关重构。

## 远程或破坏性命令

执行前：

1. 说明准确命令或操作。
2. 说明目标资源和环境。
3. 说明预期影响。
4. 获得明确批准。
5. 验证目标和凭据。
6. 只执行获批操作。
7. 报告结果。

## 内容

`main` 使用 Cloudflare-App-Base 作为通用基础品牌。具体客户或产品改动必须进入 `project/<name>` 分支。

不得编造最终法律企业名称、地址、产品、价格、推荐、认证、保证、联系人或法律文本。

## 完成声明

没有验证时不得声称测试、构建、部署、OAuth、邮箱、DNS 或安全行为正常。

诚实报告失败和限制。

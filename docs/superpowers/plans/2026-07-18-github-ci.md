# GitHub CI Implementation Plan

### Task 1: Local validation workflow

- [ ] Add a failing test that reads `.github/workflows/ci.yml` and asserts Node 22, `npm ci`, test, lint, build, and absence of deploy commands.
- [ ] Add a workflow triggered by push to `main` and pull requests.
- [ ] Run the focused test and lint; commit `ci: add local validation workflow`.

# 问答题答案文本区域设计

## 目标

为可能较长的问答题参考答案提供足够的输入空间。

## 设计

- 仅将 `free_text` 的参考答案字段从 `Input` 改为现有共享 `Textarea`。
- 默认最小高度为 `min-h-32`，并保留文本区域正常的拖动调整能力。
- 保留字段标签、提示文字、必填校验、状态、提交值和 API 请求结构。
- 不修改选择题或判断题字段。

## 验证

先添加聚焦回归测试并确认 RED，再做最小条件字段修改，最后运行完整项目检查。
